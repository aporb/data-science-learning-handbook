# Isolated Penetration Testing Environment - Requirements
# Classification: UNCLASSIFIED//FOR OFFICIAL USE ONLY
# Version: 1.0

# Core Dependencies
asyncio>=3.4.3
aiohttp>=3.8.0
aiofiles>=0.8.0
docker>=6.0.0
pyyaml>=6.0

# Security and Monitoring
psutil>=5.9.0
cryptography>=3.4.8
requests>=2.28.0
urllib3>=1.26.12

# Network and System Tools
python-nmap>=0.7.1
netfilterqueue>=0.8.1  # Optional: for advanced network monitoring
scapy>=2.4.5  # Optional: for packet analysis

# Data Processing
numpy>=1.21.0
pandas>=1.5.0
jinja2>=3.1.0

# Database (Optional)
sqlite3  # Built-in with Python
psycopg2-binary>=2.9.0  # Optional: for PostgreSQL integration
pymongo>=4.0.0  # Optional: for MongoDB integration
redis>=4.3.0  # Optional: for Redis integration

# Testing and Development
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
black>=22.0.0
flake8>=5.0.0
mypy>=0.991

# Documentation
sphinx>=5.0.0
sphinx-rtd-theme>=1.2.0

# Optional: Kubernetes Integration
kubernetes>=24.2.0  # Optional: for Kubernetes deployments

# Optional: Advanced Monitoring
prometheus-client>=0.14.0  # Optional: for Prometheus metrics
grafana-api>=1.0.3  # Optional: for Grafana integration

# Optional: Message Queue
celery>=5.2.0  # Optional: for background task processing
kombu>=5.2.0  # Optional: message queue transport

# Optional: Scheduling
schedule>=1.2.0  # Optional: for job scheduling
croniter>=1.3.0  # Optional: for cron-like scheduling

# Optional: Advanced Security
paramiko>=2.11.0  # Optional: for SSH operations
pyopenssl>=22.0.0  # Optional: for advanced SSL/TLS operations

# System Integration
systemd-python>=234  # Optional: for systemd integration (Linux only)

# Web Interface (Optional)
fastapi>=0.95.0  # Optional: for REST API
uvicorn>=0.18.0  # Optional: ASGI server
starlette>=0.25.0  # Optional: web framework

# Authentication (Optional)
python-ldap>=3.4.0  # Optional: for LDAP integration
pyjwt>=2.6.0  # Optional: for JWT token handling